<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="../../assets/stylesheets/application.css">
  <title>New Game</title>
</head>
<body>
  <div class="container pt-5">
    <div class="row justify-content-center">
      <div class="col-12 col-lg-4">
        <h2 class="text-center mb-3">💬 Longest Word Game</h2>
        <h5 class="text-center mb-5">What is the longest word you can find?</h5>
      </div>
    </div>
    <div class="row justify-content-center">
      <div class="col-12 col-lg-8">
        <div class="d-flex justify-content-center" id="cards">
          <% session[:letters].each do |letter| %>
            <div class="card">
              <div class="card-body">
                <h5 class="card-title"><%= letter.capitalize %></h5>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
    <div class="row justify-content-center">
      <div class="col-12 col-lg-8">
        <form action="/score" method="post" data-turbo="false" class="d-flex mb-32">
          <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
          <input class="form-control me-2" type="text" name="new">
          <input class="btn btn-primary" type="submit" value="Send" disabled>
        </form>
      </div>
    </div>
    <div class="row justify-content-center">
      <div class="col-12 col-lg-4 text-center">
        <h3 class="mb-3"><%= @message %></h3>
        <div class="d-flex justify-content-center" id="result">
          <h5><%= @answer %></h5>
          <h5 class="weight-normal"><%= @result %></h5>
        </div>
        <%= link_to "Play again", new_path, class: "btn btn-primary mt-3" %>
      </div>
    </div>
  </div>
  <footer class="fixed-bottom p-3">
    <div class="container d-flex justify-content-end">
      <p class="text-muted">Your score: <%= session[:score] %></p>
    </div>
  </footer>
</body>
</html>
